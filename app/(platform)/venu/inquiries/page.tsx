// Venu Inquiries tab — placeholder.
// Session 15: New / Quoted / Held / Closed segment, inquiry rows with SLA dots
// and qualification badges. Uses lib/labels/inquiry-status.ts when wired
// (PARKING_LOT #42 — Lock 15 audit: `inked` likely needs to read as `signed`).
export default function VenuInquiries() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Venu · Inquiries</h1>
      <p>New / Quoted / Held / Closed segment with SLA dots and qualification badges.</p>
    </main>
  );
}
