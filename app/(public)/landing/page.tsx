export default function LandingPlaceholder() {
  return (
    <main style={{ padding: "var(--sp-12) var(--sp-6)", maxWidth: 720, margin: "0 auto" }}>
      <h1 style={{ fontSize: "var(--fs-4xl)", color: "var(--rose)", marginBottom: "var(--sp-6)" }}>
        EvntCue
      </h1>
      <p style={{ fontSize: "var(--fs-lg)", color: "var(--txt2)", lineHeight: 1.6 }}>
        Every handoff invisible. Landing placeholder — Phase 2 builds this out.
      </p>
      <p className="ui-label" style={{ marginTop: "var(--sp-8)" }}>
        Phase 1 smoke test — Cormorant italic above, Barlow body here, Barlow Condensed UI label.
      </p>
    </main>
  );
}
