import Calculator from "./Calculator";

export const metadata = {
  title: "Budget Calculator · EvntCue",
  description:
    "Estimate your event budget — DFW-anchored line items, live totals, per-guest cost. Save and continue when you're ready to plan.",
};

export default function BudgetCalculatorPage() {
  return <Calculator />;
}
